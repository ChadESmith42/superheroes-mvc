@model SuperheroesInc.DATA.EF.CourseCharacter

@{
    ViewBag.Title = "Remove Instructor From Course";
}

<h2>@ViewBag.Title</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Character.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Character.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Cours.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Cours.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> &ensp;
            @Html.ActionLink("Instructor List", "Index", null, htmlAttributes: new { @class = "btn btn-primary" })
        </div>
    }
</div>
