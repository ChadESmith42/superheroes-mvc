@model IEnumerable<SuperheroesInc.DATA.EF.CourseType>

@{
    ViewBag.Title = "Course Types";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("New Course", "Create", null, htmlAttributes: new { @class = "btn btn-success"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeID }, htmlAttributes: new { @class = "btn btn-warning"}) &ensp;
            @Html.ActionLink("Details", "Details", new { id=item.TypeID }, htmlAttributes: new { @class = "btn btn-primary" }) &ensp;
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeID }, htmlAttributes: new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
